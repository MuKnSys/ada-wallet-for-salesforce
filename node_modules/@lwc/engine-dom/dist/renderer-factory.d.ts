import type { RendererAPI } from '@lwc/engine-core';
export type SandboxableRendererAPI = Omit<RendererAPI, 'createCustomElement' | 'insertStylesheet' | 'isSyntheticShadowDefined' | 'defineCustomElement' | 'startTrackingMutations' | 'stopTrackingMutations'>;
export type RendererAPIType<Type> = Type extends RendererAPI ? RendererAPI : SandboxableRendererAPI;
/**
 * A factory function that produces a renderer.
 * Renderer encapsulates operations that are required to render an LWC component into the underlying
 * runtime environment. In the case of @lwc/enigne-dom, it is meant to be used in a DOM environment.
 * @param baseRenderer Either null or the base renderer imported from 'lwc'.
 * @returns The created renderer
 * @example
 * import { renderer, rendererFactory } from 'lwc';
 * const customRenderer = rendererFactory(renderer);
 */
export declare function rendererFactory<T extends RendererAPI | null>(baseRenderer: T): RendererAPIType<T>;
//# sourceMappingURL=renderer-factory.d.ts.map