import prettier from "prettier";
import * as jorje from "../vendor/apex-ast-serializer/typings/jorje.d.js";
import { SerializedAst } from "./util.js";
export type EnrichedIfBlock = jorje.IfBlock & {
    ifBlockIndex: number;
};
export default function parse(sourceCode: string, options: prettier.RequiredOptions): Promise<SerializedAst | Record<string, never>>;
